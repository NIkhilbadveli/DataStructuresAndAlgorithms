"""
If you push beyond the allocated space for the stack you get a “stack overflow” error,
and if you pop when the stack is empty, you get a “stack underflow” error.

Peek returns the value of the top element of the stack without changing the stack
"""