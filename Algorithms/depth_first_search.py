"""
Used with binary trees*

At each node, the DFS will descend as far down the left branch as possible until it reaches a leaf.
Then it will step back, go down one right branch, and then back to left branches.
It sticks to the left until it reaches a leaf, and then it steps back and goes right.

# breadth_first_search
"""